<!-- Navbar -->
<nav class="navbar-modern">
    <div class="nav-container">
        <a href="#" class="nav-logo">MyApp</a>
        <ul class="nav-menu">
            <li><a href="#features" class="nav-link">Features</a></li>
            <li><a href="#" class="nav-link">Solutions</a></li>
            <li><a href="#" class="nav-link">Pricing</a></li>
            <li><a href="#" class="nav-link">About</a></li>
            <li><a href="#" class="nav-cta">Get Started</a></li>
        </ul>
    </div>
</nav>

<!-- Hero Section -->
<section class="hero">
    <div class="hero-container">
        <div class="hero-content">
            <div class="hero-badge">
                <i class="ti ti-sparkles"></i> Introducing Next-Gen Platform
            </div>
            <h1 class="hero-title">Transform Your Workflow Into Something Extraordinary</h1>
            <p class="hero-subtitle">
                Experience the future of productivity with our cutting-edge platform. 
                Built for teams who demand excellence and innovation at every step.
            </p>
            <div class="hero-buttons">
                <a href="#" class="btn-primary-hero">
                    <i class="ti ti-rocket"></i>
                    <span>Start Free Trial</span>
                </a>
                <a href="#" class="btn-secondary-hero">
                    <i class="ti ti-play"></i>
                    <span>See It In Action</span>
                </a>
            </div>
        </div>
        <div class="hero-image">
            <div class="hero-image-wrapper">
                <div class="hero-image-placeholder">
                    <i class="ti ti-device-desktop" style="font-size: 4rem;"></i>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Features Section -->
<section class="features" id="features">
    <div class="features-container">
        <div class="section-header scroll-reveal">
            <div class="section-badge">Why Choose Us</div>
            <h2 class="section-title">Built For Modern Teams</h2>
            <p class="section-description">
                Everything you need to build, ship, and scale your products faster than ever before.
            </p>
        </div>
        <div class="bento-grid">
            <div class="bento-item bento-large scroll-reveal">
                <div class="bento-icon">
                    <i class="ti ti-rocket"></i>
                </div>
                <h3 class="bento-title">Lightning Fast Performance</h3>
                <p class="bento-description">
                    Experience unparalleled speed with our optimized infrastructure. 
                    Built on cutting-edge technology that ensures your applications run at peak performance, 
                    handling millions of requests with ease and delivering results in milliseconds.
                </p>
            </div>
            <div class="bento-item bento-small scroll-reveal">
                <div class="bento-icon">
                    <i class="ti ti-shield-check"></i>
                </div>
                <h3 class="bento-title">Enterprise Security</h3>
                <p class="bento-description">
                    Bank-level encryption and security protocols protect your data 24/7.
                </p>
            </div>
            <div class="bento-item bento-small scroll-reveal">
                <div class="bento-icon">
                    <i class="ti ti-device-analytics"></i>
                </div>
                <h3 class="bento-title">Real-Time Analytics</h3>
                <p class="bento-description">
                    Get instant insights with powerful analytics that help you make data-driven decisions.
                </p>
            </div>
            <div class="bento-item bento-medium scroll-reveal">
                <div class="bento-icon">
                    <i class="ti ti-brand-github"></i>
                </div>
                <h3 class="bento-title">Seamless Integrations</h3>
                <p class="bento-description">
                    Connect with your favorite tools and services. Our platform integrates with over 100+ 
                    popular applications, making it easy to fit into your existing workflow without disruption.
                </p>
            </div>
            <div class="bento-item bento-medium scroll-reveal">
                <div class="bento-icon">
                    <i class="ti ti-users"></i>
                </div>
                <h3 class="bento-title">Team Collaboration</h3>
                <p class="bento-description">
                    Work together in real-time with advanced collaboration features. Share, comment, 
                    and iterate with your team members across the globe effortlessly.
                </p>
            </div>
        </div>
    </div>
</section>

<!-- Stats Section -->
<section class="stats">
    <div class="stats-container">
        <div class="stats-grid">
            <div class="stat-item">
                <div class="stat-number">500K+</div>
                <div class="stat-label">Active Users</div>
            </div>
            <div class="stat-item">
                <div class="stat-number">99.9%</div>
                <div class="stat-label">Uptime</div>
            </div>
            <div class="stat-item">
                <div class="stat-number">150+</div>
                <div class="stat-label">Countries</div>
            </div>
            <div class="stat-item">
                <div class="stat-number">50M+</div>
                <div class="stat-label">API Calls/Day</div>
            </div>
        </div>
    </div>
</section>

<!-- CTA Section -->
<section class="cta">
    <div class="cta-container">
        <h2 class="cta-title">Ready to Get Started?</h2>
        <p class="cta-description">
            Join thousands of teams already using our platform to build amazing products. 
            Start your free trial today, no credit card required.
        </p>
        <div class="cta-buttons">
            <a href="#" class="btn-cta-white">
                <i class="ti ti-check"></i>
                <span>Start Free Trial</span>
            </a>
            <a href="#" class="btn-cta-outline">
                <i class="ti ti-message-circle"></i>
                <span>Talk to Sales</span>
            </a>
        </div>
    </div>
</section>

<!-- Footer -->
<footer class="footer">
    <div class="footer-container">
        <div class="footer-content">
            <div>
                <div class="footer-brand">MyApp</div>
                <p class="footer-description">
                    Building the future of productivity, one innovation at a time. 
                    Empowering teams worldwide to achieve more.
                </p>
            </div>
            <div>
                <h4 class="footer-title">Product</h4>
                <ul class="footer-links">
                    <li><a href="#">Features</a></li>
                    <li><a href="#">Pricing</a></li>
                    <li><a href="#">Security</a></li>
                    <li><a href="#">Roadmap</a></li>
                </ul>
            </div>
            <div>
                <h4 class="footer-title">Company</h4>
                <ul class="footer-links">
                    <li><a href="#">About</a></li>
                    <li><a href="#">Blog</a></li>
                    <li><a href="#">Careers</a></li>
                    <li><a href="#">Contact</a></li>
                </ul>
            </div>
            <div>
                <h4 class="footer-title">Resources</h4>
                <ul class="footer-links">
                    <li><a href="#">Documentation</a></li>
                    <li><a href="#">API Reference</a></li>
                    <li><a href="#">Support</a></li>
                    <li><a href="#">Status</a></li>
                </ul>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2025 MyApp. All rights reserved.</p>
        </div>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/@tabler/core@latest/dist/js/tabler.min.js"></script>
<script>
    // Navbar scroll effect
    const navbar = document.querySelector('.navbar-modern');
    window.addEventListener('scroll', () => {
        if (window.scrollY > 100) {
            navbar.classList.add('scrolled');
        } else {
            navbar.classList.remove('scrolled');
        }
    });

    // Scroll reveal animation
    const revealElements = document.querySelectorAll('.scroll-reveal');
    
    const revealOnScroll = () => {
        revealElements.forEach(element => {
            const elementTop = element.getBoundingClientRect().top;
            const elementVisible = 150;
            
            if (elementTop < window.innerHeight - elementVisible) {
                element.classList.add('revealed');
            }
        });
    };

    window.addEventListener('scroll', revealOnScroll);
    revealOnScroll(); // Check on load

    // Smooth scrolling for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            const href = this.getAttribute('href');
            if (href !== '#' && href !== '') {
                e.preventDefault();
                const target = document.querySelector(href);
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            }
        });
    });

    // Animate stats on scroll
    let statsAnimated = false;
    const statsSection = document.querySelector('.stats');
    const animateStats = () => {
        if (statsAnimated) return;
        
        const statNumbers = document.querySelectorAll('.stat-number');
        const statsSectionTop = statsSection.getBoundingClientRect().top;
        
        if (statsSectionTop < window.innerHeight - 200) {
            statsAnimated = true;
            statNumbers.forEach(stat => {
                const text = stat.textContent;
                const hasPlus = text.includes('+');
                const hasPercent = text.includes('%');
                const number = parseInt(text.replace(/[^0-9]/g, ''));
                
                let current = 0;
                const increment = number / 50;
                const timer = setInterval(() => {
                    current += increment;
                    if (current >= number) {
                        current = number;
                        clearInterval(timer);
                    }
                    
                    let displayText = Math.floor(current).toLocaleString();
                    if (hasPlus) displayText += '+';
                    if (hasPercent) displayText += '%';
                    if (text.includes('K')) displayText = Math.floor(current) + 'K+';
                    if (text.includes('M')) displayText = Math.floor(current) + 'M+';
                    
                    stat.textContent = displayText;
                }, 30);
            });
        }
    };

    window.addEventListener('scroll', animateStats);
    animateStats();
</script>
