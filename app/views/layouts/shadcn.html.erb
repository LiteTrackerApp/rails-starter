<!DOCTYPE html>
<html lang="en" class="light" style="color-scheme: light;">
  <head>
    <title><%= content_for?(:title) ? "#{yield(:title)} - #{Rails.application.class.module_parent.name}" : Rails.application.class.module_parent.name %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="utf-8">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= vite_client_tag %>
    <%= vite_react_refresh_tag %>
    <%= vite_javascript_tag "shadcn.jsx" %>
  </head>

  <body class="min-h-screen bg-background font-sans text-foreground antialiased">
    <script type="application/json" id="shadcn-root-props">
      <%= raw({ currentUser: user_signed_in? ? { name: current_user.name.presence || current_user.email, email: current_user.email, profilePath: edit_user_path(current_user), logoutPath: destroy_user_session_path, avatarUrl: "/avatars/default.jpg" } : nil }.to_json) %>
    </script>
    <div id="shadcn-root">
      <%= yield %>
    </div>
  </body>
</html>
